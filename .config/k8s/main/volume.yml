apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: radar-db-prod
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  storageClassName: do-block-storage
